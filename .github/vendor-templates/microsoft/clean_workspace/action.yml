
inputs:
  system:
    description: 'Operating system'
    required: true
    type: string

runs:
  using: 'composite'
  steps:
    - name: clean up workspace (macOS)
      working-directory: ${{ github.workspace }}
      shell: bash
      run: |
        rm -rf ./*
        rm -rf ./.??*